/*
 * This enum MUST match that order that exists in the build settings.
 * This is used to have an easy visualizer for picking a scene on the level trigger script
 */

/// <summary>
/// An enum of the Scenes which can be found in File -> Build Settings -> Scenes in Build
/// </summary>
public enum SceneIndexes
{
	LoadingScene,
	Town1,
	TrialDungeon
}
